"""
Evaluation tooling (manual, run outside the live system).

Standalone benchmark scripts only — compute_metrics.py, coverage_matrix.py,
analyze_benchmark.py, etc. The in-system evaluation API and the legacy
MetricsCalculator engine were removed; the scanner is general-purpose and
thesis evaluation is computed manually from these scripts.
"""
